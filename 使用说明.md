# 董解析 0.23.0 使用说明

正式网页：https://1157364449han-max.github.io/-/

0.23.0 已加入独立云端解题接口。管理员完成云主机、模型 API 和访问授权配置后，网站会直接使用云端 AI 处理整题。当前公开站尚未接通真实服务，不能把接口已完成理解为 AI 已可用。

已有本机版的用户仍可运行“安装本地AI.bat”，然后检测并自愿下载模型。云端拥堵时可主动改用本机版；程序不会自动下载模型，也不会把选择本机处理的题目转发云端。手机不需要安装模型。首次免环境桌面安装包仍待完善。

0.22.0 新增统一 LaTeX 显示和圆、轴对齐椭圆的外点切线计算。此前的切题保护继续保留。当前纯网页版尚未接入真实通用云服务，不要把支持部分确定性解法理解为能自主解答所有新题。

## 公式输入与排版

- 题目、答案、解析、追问、题本和动态面板中的公式可使用 `$...$`、`$$...$$`、`\(...\)`、`\[...\]`；打印保留排版。
- “题目公式预览”可检查输入效果。主曲线方程、建图模板和对象方程使用数学排版；模板参数旁显示实时公式预览，输入框仍保留可编辑源码。
- 例如题目可以输入 `椭圆 $\frac{x^2}{9}+\frac{y^2}{4}=1$`。显示正确不代表任意复杂表达式都已具备求解支持。
- Canvas 中的普通点名仍为原生文字，目前不支持将任意复杂 LaTeX 公式作为画板标签直接导出为图片。
- 外点切线可输入“已知椭圆 x²/9+y²/4=1，过点 P(5,0) 作两条切线，切点分别为 A、B，求切线方程和三角形 PAB 的面积”。使用一般公式计算，不依赖该组数值；坐标小数明确标注为近似值。

本版把原有本机程序整理为可安装的渐进式 Web 应用（PWA）：同一套中文界面可运行在电脑浏览器、手机和平板上，并保留本机 AI、原生动态画板与“答案可信度”层。公开部署后，手机端不需要安装 Python、SymPy 或模型；新增版本会由应用检测并提示刷新，电脑与手机可从同一次发布获得更新。

## 原位编辑、切线与法线（0.21.0）

- 双击手动添加的曲线，或在“参数 / 图层”点该曲线的“编辑”，即可重新填写参数。曲线上的点和由它构造的线会保留并一起更新，不需要删除重画；取消不会修改图形。
- 点“切线”或“法线”，先选曲线，再选曲线上的位置或已有点。鼠标悬停可预览，点击后生成真实关联构造；拖动切点或点“播放联动”可演示变化。
- 若提示“点不在曲线上”，请检查选中的曲线及点，不能把曲线外的点直接当成切点。本工具不是“过任意外点求所有切线”。
- 答案已有切线/法线现在也跟随合法源点变化；若点移出曲线，线暂时不画并给出原因，恢复条件后自动出现。原题解析对应原始题设，不会因拖图而自动改写证明。

## 在原图上继续作图

- 追加曲线：用画板上方“＋椭圆 / ＋双曲线 / ＋抛物线 / ＋圆”，填写模板参数，点“添加到画板”。原题、原曲线和解析会保留；左侧“直接新建”用于新场景，不是追加。
- 过定点作线：选“过点直线”，先点要经过的点，再点方向。拖动这条线只改变方向；选中后在“参数 / 图层”的对象属性内可填写倾斜角（单位为度）。若要一直同时经过两个动点，请用“两点直线”。
- 吸附：勾选“点线吸附”，靠近已有点、交点或曲线时会有绿色提示。点工具吸附到曲线后生成曲线上点，移动曲线时点也会跟随；“播放联动”可以演示曲线上的动点。取消勾选可自由落点，“网格 0.5”单独控制。
- 隐藏与保存：曲线可按小问显示或在图层隐藏，隐藏对象不参与吸附；“撤销 / 重做”和保存图稿保留这些构造关系。现支持直线与任意已添加圆锥曲线求交、圆与圆求交，尚不支持通用圆锥曲线之间求交。

## 先看核验状态，再看答案

解答顶部会显示“仅生成 · 待核验”“局部代数核验通过”或“发现确定性冲突”。“已生成完整作答”只说明模型写出了本问，不代表结论正确；只有展开的机器核验项目才是程序实际复算过的关系。

当前可确定性复核主曲线参数、题设点代回、固定直线与圆锥曲线的交点关系、判别式和真正的二次重根。一个实交点若来自联立降为一次，程序会明确说明不能据此判相切。定点、定值、最值和范围题会列出尚需完成的证明义务，但现阶段仍不能保证完整证明均已机器验证。

“结构化题目模型”折叠区记录曲线、小问目标和题设对象；图层会标注对象来自题干、规则、推导、AI 或手动构造。若题图识别文字仍含 `[看不清]` 等模糊字段，必须先在确认窗口补正，程序不会带着不确定题面继续解题。

## 学生：先独立思考，再看提示

载入解析后，在右侧“学习方式”选择“先思考 · 逐步提示”。结论和推导会先隐藏，点击“给我第一步提示”或“展开下一步”逐步查看；“显示结论”和“重新尝试”独立控制答案。每个小问的展开进度分别保存。切回“完整解析”可恢复完整阅读和原有复制、打印功能。提示来自本题已有解析，不是额外生成的分层提示，个别步骤本身可能包含答案。

打开“我的思路与错题记录”，写下自己的尝试、卡住的位置或错因。点击“请 AI 点评我的思路”会把原题、已有解析和你的尝试交给本机模型，点评显示在追问区；模型未就绪时会明确提示。它用于学习反馈，不是可靠的考试评分器。

掌握情况可选“加入错题 · 需要复习”或“已掌握”。我的题本新增对应筛选，搜索也会匹配笔记。笔记、掌握情况和展开进度保存在当前浏览器并随题稿导出，可导入另一台设备。

## 老师：课堂展示与练习页

点击“进入讲题模式”，只保留图形和讲解区，自动隐藏结论并从第一步开始。顶部可切换小问，点击上下步，或用左右方向键、PageUp / PageDown 逐步展示；输入框和下拉框获得焦点时保留其原有键盘行为。字号有 18、22、26 px 三档，可进入浏览器全屏；退出按钮或 Esc 返回普通布局。

“红笔批注”开启后，可用鼠标或触摸在图形上标记，支持撤销和清除；关闭红笔可继续拖动几何对象。批注是屏幕标记，不附着于几何坐标，平移/缩放画面后可能需要清除重画；它不保存到题稿，也不包含在图稿导出或打印中。切换到另一道题时会清除批注。个人笔记在讲题模式中隐藏。

“打印练习页”只打印原题文字和空白作答区，不包含模型推导、答案或自动生成的解题图，方便发给学生。完整解析仍可在完整阅读模式中“打印 / PDF”。

## 输入完整题目，一站式解答

1. 输入完整题目，保留（1）（2）等小问编号，点击“一站式解题”。不需要先求出标准方程。
2. 程序先运行内置确定性引擎。当前可由离心率、轴长、焦点、过点、圆心、直径、顶点与准线等已覆盖条件反推曲线，并计算特征量、固定直线交点、弦长和中点；这些功能不要求下载 Qwen、Ollama 或其它模型。
3. 解答后右侧优先显示解析，顶部可切换“解析 / 追问”和“参数 / 图层”。点击“展开解析”获得大窗口阅读。小问按钮同步切换图层；“全部解析 / 图层”展示全题。
4. 未覆盖的定点、定值、最值、轨迹或综合证明会标为“还需继续推理”，不会用猜测伪装成完整答案。若管理员已经配置独立的智能增强服务，程序才会在确定性结果之后补充开放题推导。
5. 图片需先“添加题图 → 识别题图 → 核对识别文字 → 一站式解题”，特别检查分数、正负号和小问编号。

## 答案推导出的图形

对“抛物线焦点 P 的动弦 AB—两端点切线—切线交点 Q—弦中点 M”这一常见定点定值链，画板会建立完整的实时依赖：转动 l 时，A、B、弦、两条切线、Q 和 M 同步变化；Q 的准线轨迹与 M 的抛物线轨迹是可单独隐藏的推导图层。点击“全部解析 / 图层”可同时查看全题对象，点击某一小问只显示与该问相关的轨迹。

在电脑端，完成题目导入后可点击画板右上角“隐藏题目栏”，解析列和画板会自动占用空出的宽度；按钮随即变为“显示题目栏”，可随时恢复后输入下一题。手机端为了避免题目和输入入口丢失，不会隐藏该栏。

画板不再只显示题干里已经写出的对象。当前若题目要求圆、椭圆、双曲线或抛物线在曲线上点处的切线或法线，内置引擎会由主曲线的精确方程求梯度、生成直线方程，并把它加入对应小问的画板图层。切点既可以是题干直接给坐标的点，也可以是前一问或当前问由“直线与曲线联立”精确求出的交点；切线会引用真实交点对象而不是只依赖名称。切换小问时，这些答案派生图形随小问显示或隐藏；切点不在曲线上或条件不足时不会强行画近似直线。

若题目要求证明 A、B 两点处的切线互相垂直，程序会同时生成并显示两条切线，列出两式，再用一般式系数构造方向向量并计算点积。机器核验会独立确认两条线引用不同切点、各自确为切线以及方向向量点积为 0；若当前数据算出的点积不为 0，会显示确定性冲突而不是照抄题目结论。

题目明确给出点、直线方程和垂足名称时，例如“点 P(3,4) 到直线 l:y=1 的垂足为 H”，程序会使用正交投影求出 H，把 H 作为依赖源点和直线的动态构造加入画板，并分别验证 H 在直线上以及 PH 与直线垂直。拖动可编辑的源点或直线时，H 会随之重算。

固定直线与圆锥曲线相交时，答案中求出的交点、弦和中点也会成为画板中的“推导对象”。例如“圆 x²+y²=9 与 y=0 交于 A、B，求弦长和中点”，画板会显示 A、B、弦 AB 和中点 M；每个对象绑定对应小问，在线与离线解题使用相同构造契约。核验层会重新检查交点同时满足曲线和直线、端点分支顺序、弦长以及中点坐标平均，引用断裂时整条依赖链会标为冲突。

轨迹题已覆盖常见的“P 在圆锥曲线上运动，Q 为固定点，M 为 PQ 的中点，求 M 的轨迹”。程序用 `P=2M-Q` 做精确仿射消元，给出轨迹方程，并在画板中增加可单独隐藏的“ M 的轨迹 · 推导”曲线；播放联动时，M 会沿该轨迹运动。圆、椭圆、双曲线和抛物线均保留平移、方向、开口和分数参数。核验层会同时检查依赖图、消元式和实际绘制参数，不能只凭若干采样点猜测轨迹。

切线、法线、垂足和已覆盖轨迹都采用统一的 `dongjiexi-construction/v1` 派生构造数据；桌面求解与断网浏览器求解会运行同题一致性测试。当前尚未覆盖从圆锥曲线外一点作两条切线、任意证明步骤中的所有辅助圆及任意关系的通用轨迹消元；未覆盖轨迹会明确保持“未完成”，不会用拟合曲线冒充精确答案。

生成答案不等于数学证明已通过检验。复杂定点、最值、参数范围仍可能出错；请检查每步推导和机器核验状态。题干、确定性计算、分步答案与画板使用同一场景数据；若智能增强给出的曲线参数和题干冲突，画板会丢弃冲突参数并按题干重建。手动添加的对象只在依赖链完整时恢复；若其依赖的 AI 猜测对象已被丢弃，界面会明确提示该孤立构造没有恢复。

## 解题引擎与可选智能增强

内置确定性解题和动态画板是默认路径，不要求用户安装大模型。当前电脑已有的 Ollama / Qwen 只作为开放题“智能增强”，不是启动软件或完成已覆盖题型的前置条件；它们移入折叠的可选设置区，卸载或未启动时不影响内置解题。

手机和公开网页默认直接在浏览器运行已覆盖题型的内置解题与动态画板，不依赖这台电脑常期开机。未配置独立云端 API 时，图片识别与开放题 AI 增强不可用，但不妨碍内置解题。私人或课堂的云端增强部署可输入教师提供的访问口令，浏览器只换取并临时保存短期会话，不保存服务器密钥。面向公众开放 AI 增强前还需要正式身份认证、云端限流与费用控制；是否已经上线须以部署成功和公网访问检查为准。

其他电脑或手机访问已发布的 Web/PWA 版本时，已覆盖的浏览器解题和离线画板不需要 Python、SymPy 或模型。当前桌面开发版仍由随项目配置的本地服务启动；真正面向公众的开放题智能增强不能把这台电脑当服务器，必须单独部署受保护的 HTTPS 服务。

## 题本、保存和打印

完成 AI 解答后自动存入“我的题本”，也可手动保存；最多保留 50 道不同题目，同题保存会更新。题本保存在当前浏览器，不是服务器同步库，清理浏览器数据会丢失，请导出题稿备份。题稿包括题干、图形、小问解析和追问，不包含题图原文件。可搜索、打开、导出及导入（单题文件上限 5 MB）。仅含文字解析的题目也能保存和恢复。

“打印 / PDF”输出全题解析和当前画板图，可在浏览器打印窗口另存为 PDF。调整图形后打印会注明图形已改变。修改题干时旧解析保留并提示，重新解答后才会更新。

## 启动与确认版本

双击桌面“董解析”。如果旧版启动窗口仍在运行，请先关闭旧窗口，再重新启动；浏览器按 Ctrl+F5 刷新。顶部应显示“v0.23.0 · 更新记录”。

仍使用原安装目录 D:\智几何 和原桌面快捷方式。运行时保留启动窗口，关闭它会停止本地服务。发布 ZIP 是当前 Python 程序的备份/更新包，不是独立免安装 EXE；当前电脑依赖已经就绪，其他电脑需要 Python 与 SymPy。

## 不输入题目也能直接作图

右侧“自由作图与图层”默认使用参数模板：先选点、直线、圆、椭圆、双曲线、抛物线或函数曲线，再选方程/条件模板，只填写显示出来的参数格。选择坐标点时直接填写 x、y，不需要输入 `P(1,2)`。参数支持整数、小数、分数（如 `3/2`）、`π`、根号和四则运算；绿色区域会先显示程序自动生成的方程。

当前模板包括直线斜截式、竖直线、点斜式、一般式、两点式；圆心半径、圆心过点、直径端点、三点定圆；椭圆/双曲线的半轴、焦距、离心率；抛物线的顶点焦参数和顶点焦点；以及二次、三次、绝对值、反比例、指数、对数、正弦、余弦、正切和根式函数。需要粘贴现成方程时，展开“高级：直接输入完整方程”。

题干、主曲线参数、方程模板参数、识别确认字段和高级方程输入旁的“数学键盘”完全在浏览器本地工作，包含常用符号、函数、圆锥方程、希腊字母和集合逻辑符号；按 `Ctrl+Shift+K` 可开关。键盘会把内容插入当前区域最后获得焦点的输入框。

点击主画板上方工具，再点击画板。当前步骤提示显示在工具栏下方。第一步选错时按 Esc 取消，未完成构造中的临时点不会保存。

| 工具 | 操作 |
| --- | --- |
| 点 | 点空白处建立自由点；点已有直线、圆或主圆锥曲线，建立其上的受约束动点 |
| 直线 / 线段 / 射线 | 依次选两个点；点击空白处会自动创建所需点 |
| 圆 | 先选圆心，再选圆周上的一点 |
| 中点 | 依次选择两个端点 |
| 垂线 / 平行线 | 先选经过的点，再点参照直线 |
| 交点 | 依次点两个图形：线线、线圆、圆圆，或直线与主圆锥曲线 |
| 垂足 | 先选待投影的点，再点目标直线；使用直线的延长线 |
| 测距 | 依次选两个点，距离会标在画板上 |

作图完成后，可继续使用同一工具；点“选择 / 拖动”或按 Esc 返回拖动模式。

## 拖动与依赖关系

- 题干写明“P 为曲线上的动点”“M 为 PF₁ 的中点”“连接 PM”等常见关系时，会直接生成受约束动点、中点和派生线；P 移动后其后代对象一起重算。
- 点击画板“▶ 播放联动”可连续驱动题设动点和动直线，交点、中点及派生线会实时跟随；再次点击暂停并保留当前位置。切换题目、清空画板或页面进入后台会自动停止。
- 拖动自由点，关联直线、圆、中点、垂线、交点与测量值会实时重算。
- 圆、直线、主圆锥曲线上的动点拖动时仍保持在对应图形上。
- 中点、垂足与交点由源对象决定，不能直接拖离构造关系；点击它们后，右侧会显示依赖。
- 两点构造的直线、线段、射线或圆，在所需源点均为自由点时可拖动整体平移。否则请拖动其自由源点。
- 平行线和垂线可随其自由经过点平移，但方向始终受参照直线约束。
- 输入方程添加的独立直线，拖线身平移，选中后拖旋转柄改变方向。主曲线中心、顶点等旧版控制点仍然可用。
- 图形不再相交、源对象重合或被移除时，相应依赖对象标记为“暂未定义”，不会伪造坐标；恢复条件后会自动重现。
- 鼠标滚轮缩放；拖空白处平移视野。吸附选项帮助对齐 0.5 格坐标。

这些动态计算用于构造和探索，不是对任意题目的自动证明。拖动改变原题条件后，原文字解析仍针对原始条件。

## 选中对象、图层与小问

右侧“选中对象与构造关系”显示坐标或方程。可修改名称、隐藏/显示对象、绑定到某个已识别小问。

隐藏源对象不会破坏依赖构造。删除源对象时，会明确显示一并删除的关联对象数量；“撤销”或 Ctrl+Z 可恢复整组。Ctrl+Y 可重做。操作历史只在当前会话保留，重新打开程序不恢复旧撤销栈。

原有方程输入也保留：可输入 y=2x+1、x=2、x²+y²=9、(x-1)²+(y+2)²=25、P(1,2) 等。当前仍不是任意隐式方程绘图器。

## 保存与打开图稿

“保存图稿”导出 JSON 文件，保留坐标、对象 ID、构造依赖和图层设置。“打开图稿”选择这个文件，即可继续拖动编辑。

画板也会自动保存在当前浏览器、当前访问地址下。“保存到本机”仅写入浏览器存储，不是文件备份。更换浏览器、端口、设备或清理浏览器数据可能找不到原草稿，因此重要作图请导出 JSON。

旧版 .ggb 草稿在右侧底部折叠的“旧版 .ggb 草稿（可选）”中打开。该旧编辑器仍需联网；它与新的本地 JSON 构造是独立图稿，不能自动互转或双向同步。本次新增构造不应通过旧编辑器导出备份。

## 解题与适用范围

当前版本的重点是把动态作图、整题解答、分步教学和确定性核验组织成统一流程，但它仍不是已经证明所有结论的通用证明器。规则解题支持部分标准式和条件反推；AI 回答仍须结合顶部核验状态逐步检查。

尚未内置：两条任意圆锥曲线求交、三维构造、通用符号代数、任意高考题的完整正确证明。没有把 GeoGebra 全部功能复制进本软件。

## 手机与多媒体

电脑浏览器可直接投屏到多媒体。PWA 已通过 Microsoft Edge 的离线重载、触摸布局和 390 像素窄屏自动化验收，仍建议在正式发布前补做常见 iPhone、安卓手机和平板的真机验收。

正式云端部署后，手机只需打开同一个 HTTPS 地址；在浏览器菜单选择“添加到主屏幕”或点击页面的“安装应用”，即可像普通应用一样启动。应用外壳和画板资源可离线打开；需要 AI 的功能必须联网连接云端解题引擎。每次打开、重新回到页面以及定时检查时都会查询新版本，发现更新后由用户确认刷新，不会在做题过程中强制重载。

目前还没有可声明为已上线的公网地址，因为本机尚未具备目标 GitHub 仓库的推送身份与可用连接。项目已经包含 GitHub Pages 工作流、容器部署文件、PWA 缓存和更新机制；完成一次仓库授权和云端引擎部署后，电脑与手机将从同一版本源同步更新。

临时体验仍可在可信私人网络中运行“手机访问.bat”，同一 Wi-Fi 的手机打开窗口给出的地址。此模式依赖电脑保持开机，且没有公网级账号体系，只能用于家庭或课堂的可信网络，禁止把端口直接暴露到互联网。题本和草稿当前保存在各自浏览器，不会跨设备自动上传；可通过题稿或 JSON 文件迁移。

## 原地更新

关闭启动窗口后运行“安装更新包.bat”，选择可信 ZIP 包，或把 ZIP 拖到该批处理文件上。更新器会保留带时间戳的旧版完整备份，不需要更换桌面快捷方式。

桌面更新包和手机网页必须从同一次版本发布生成。网页端读取 `app-version.json` 并提示更新；桌面端仍可关闭启动窗口后运行“安装更新包.bat”原地升级，桌面快捷方式无需重建。

“检查更新.bat”在配置 HTTPS 发布地址后提供在线检查。当前源码已准备自动发布工作流，但由于尚未获得 GitHub 推送授权，在线发布通道还没有实际启用。

## 第三方声明

新的主画板构造模块是本地实现，不依赖 GeoGebra 在线应用。旧版兼容编辑器仍通过 GeoGebra 官方接口按需加载，不在 ZIP 中复制其应用资源。GeoGebra 名称、标识及应用归各自权利人所有，使用应遵守其许可。

- 嵌入文档：https://geogebra.github.io/docs/reference/en/GeoGebra_Apps_Embedding/
- 许可：https://www.geogebra.org/license
