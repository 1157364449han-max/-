# 董解析更新日志

每轮有效改进发布一个可核查版本；先验证再发布，不以修改版本号冒充功能进展。软件顶部版本号可直接打开更新记录。

## 0.21.1 · 2026-09-23（北京时间）

- 修复解题过程中修改题目后，迟到的旧题结果覆盖当前画板、解析和题本的问题；旧题结果丢弃并明确提示，也不继续追加旧题增强请求。
- 防止处理期间重复提交，服务状态检查失败时保留解题按钮的忙碌状态。
- 纯网页版完成解题后，未配置的图片识别与模型服务按钮保持禁用，内置解题可继续使用。
- 功能修改：`dist/learning-ui.js`。新增 `tests/solve_switch_browser_smoke.cjs`，接入 `tests/run_browser_smoke.cjs` 的发布回归。
- 版本、页面资源、离线缓存与测试夹具同步升级至 0.21.1；发布继续使用现有 GitHub Pages 地址。
- 未扩张新题型覆盖；任意复杂证明与未配置的 AI 增强仍保持原有限制。

## 0.21.0 · 2026-09-23（北京时间）

- 新增手动曲线原位编辑：双击曲线、点对象属性“编辑曲线参数”或点图层“编辑”，重新填中心、半轴/半径、焦参数与方向。保留对象 ID、已有从属点线及小问归属，取消不改图，撤销重做整体恢复；不允许把题目推导曲线误当成手动曲线改写。
- 新增原生“切线 / 法线”工具：先点圆或圆锥曲线，再点曲线上一点；悬停显示预览，可复用合法已有点或自动生成曲线上点。拖动源点、改变曲线后两线同步重算，法线与切线始终垂直。
- 修复答案中部分切线/法线只保留初始斜率截距的问题：符合构造引用的答案线按当前源点及曲线重算，不再允许独立拖偏。点移出曲线时不画假切线，恢复到曲线上后自动出现；警告覆盖题设/答案直线集合，不再只检查手动对象。
- 曲线对象属性与图稿中的方程由实时参数生成，修复拖动后方程文字滞后。原题解答不被改写，探索状态明确说明当前图形已不等同于原题。
- 修复手机端无效的“隐藏题目”按钮，保留模板数学键盘，缩窄布局并增大触控按键；手机可只用屏幕键盘填写分数参数。
- 测试：62 项 Python、14 项原生几何、6 项浏览器可信契约及 PWA 契约。11 套实际 Edge 流程覆盖切法线预览、动态垂直、四类曲线编辑、稳定 ID/引用、分数根式输入、取消/撤销/重做、双击入口、错误切点提示、刷新恢复和手机触屏。手机浏览器模拟不代替真机验收。
- 新增统一发布构建器：静态网页默认禁用后端依赖；桌面包按清单打包，排除本地数据和密钥，生成同版本更新清单与 SHA-256。自动流程先运行数学、发布和浏览器测试，再尝试部署 Pages；配置错误不伪装成发布成功。

### 本阶段修改文件与后续

- 功能：`dist/index.html`、`dist/construction-board.js`、`dist/equation-builder.js`、`server.py`。
- 测试：新增 `tests/curve_edit_tangent_browser_smoke.cjs`、`tests/mobile_touch_browser_smoke.cjs`、`tests/release_build_tests.py` 和跨平台 `tests/run_browser_smoke.cjs`，扩展 `tests/native_geometry_tests.cjs` 与 `tests/verification_tests.py`，去除 `tests/browser_contract_tests.cjs` 的本机绝对路径。
- 发布：`deploy/build_release.py`、`.github/workflows/deploy-dongjiexi.yml`、`deploy/部署说明.md`、`README.md`、`.gitignore`。源代码仓库为 `1157364449han-max/-`，本机更新通道同步迁移到新仓库的预期 Pages 地址；首次部署成功前不可用。
- 版本：`version.json`、`dist/app-version.json`、`dist/releases.json`、`dist/runtime-config.js`、`dist/runtime.js`、`dist/pwa.js`、`dist/service-worker.js`、`deploy/runtime-config.web.example.js`、HTML 资源版本，以及 `tests/deployment_tests.py`、`tests/pwa_contract_tests.cjs`、`tests/focus_chord_browser_smoke.cjs`、`tests/one_stop_solver_browser_smoke.cjs`、`tests/additive_conic_snap_browser_smoke.cjs`；文档为本日志与 `使用说明.md`。
- 范围：本轮切线是“曲线上一点处的切线”，不冒充“过任意外点作全部切线”；复杂自然语言题覆盖与通用圆锥曲线两两求交仍需后续扩展。未增加网页终端运行依赖，未另建程序。构建包、上传源码与公网部署是不同状态，不以构建成功冒充正式上线。
- 后续优先项：扩展多曲线求交及相切/重合的可靠识别；增加题目对象覆盖报告，明确区分尚未建模、被隐藏和当前未定义。

## 0.20.0 · 2026-09-22（北京时间）

- 画板顶部“＋椭圆 / ＋双曲线 / ＋抛物线 / ＋圆”打开追加模板，原题、解析和主曲线不被替换；可填写根式、分数、名称与小问归属，支持撤销/重做。左侧“直接新建”仍保留新建场景语义。
- 新增“过点直线”：先吸附或新建定点，再点击方向；拖动只旋转，不平移定点。选中后可在对象属性输入倾斜角，水平、竖直、45° 方向可吸附，已有几何对象优先于方向吸附。
- 新增“点线吸附”，按已有点、交点、曲线的顺序选择；曲线上点与交点是带引用的真实构造，随源图形变化，隐藏图层不参与吸附。与原有网格 0.5 吸附独立控制。
- 附加圆锥曲线接入原生依赖引擎：独立参数化、投影、直线求交和动画播放，不再错误引用主椭圆的参数。适应视野包含已添加曲线。
- 修复新旧模板 ID 冲突、再次核验时丢失新增依赖构造，以及大幅平移导致无效切点被接受的问题。
- 验证：57 项 Python、11 项原生几何、6 项浏览器可信契约及 PWA 契约；新增真实 Edge 回归覆盖追加、键盘、约束旋转、吸附联动、隐藏/关闭吸附、撤销重做与刷新恢复。
- 范围：通用圆锥曲线之间的自动求交仍未实现（支持直线与圆锥曲线、圆与圆）；本轮不宣称扩展了任意自然语言难题的解答覆盖率。

### 本阶段修改文件与验收

- 功能代码：`dist/index.html`（追加曲线、弹窗、吸附开关、过点工具、视野与播放入口）、`dist/construction-board.js`（依赖求值、吸附、约束直线、有效切点检查）、`dist/equation-builder.js`（多实例输入 ID）、`server.py`（可信场景恢复支持）。未删除原有模块或改用外部作图网站。
- 回归测试：新增 `tests/native_geometry_tests.cjs`、`tests/additive_conic_snap_browser_smoke.cjs`；扩展 `tests/verification_tests.py`、更新 `tests/conic_quick_tools_browser_smoke.cjs` 的入口断言。
- 版本同步：`version.json`、`dist/app-version.json`、`dist/releases.json`、`dist/runtime-config.js`、`dist/runtime.js`、`dist/pwa.js`、`dist/service-worker.js`、`deploy/runtime-config.web.example.js`；对应 `tests/deployment_tests.py`、`tests/pwa_contract_tests.cjs`、`tests/focus_chord_browser_smoke.cjs`、`tests/one_stop_solver_browser_smoke.cjs` 的版本夹具及本说明文档。
- 静态前端无需 npm 构建；57 项 Python、11 项几何、6 项可信契约与 PWA 契约全部通过。实际 Edge 的 9 套流程通过：新增追加/吸附（纯 Web、禁用后端 API）、焦点弦、推导构造、一站式解题、PC 布局、依赖动画、结构化输入、快捷曲线、PWA 离线重载，未发生 JavaScript 异常。
- 协作：由另一位 Codex 审查代理独立检查数学与兼容性并编写几何回归测试，发现的重复 ID、吸附优先级、场景恢复类型以及平移容差问题均修复后通过测试；没有声称调用 DeepSeek 或透支 Copilot。
- 下一阶段：优先扩展多圆锥曲线求交与退化情况提示；改进复杂题目对象/答案对象的覆盖检测及漏图提示。本轮是本机已有项目的更新，尚未执行公开站点发布。

## 0.19.0 · 2026-09-22（北京时间）

- 修复完整题目中“过 P 的直线 l”被错误绑定到第（4）问的问题；题干先定义的 l、A、B 与弦 AB 现在是全局对象，切换任一小问都不会只剩主抛物线。
- 新增原生实时切线构造：A、B 是 l 与抛物线的动态交点，两条切线由当前切点的隐式梯度即时重算；旋转或播放 l 时，切线、交点 Q 和中点 M 保持完整依赖关系。
- 对抛物线焦点弦题建立确定性解答链：由 `t₁t₂=-1` 证明切线垂直，求得 Q 的轨迹是准线 `x=-1`，M 的轨迹为 `y²=2(x-1)`；`P∈AB`，所以题设 `△PAB` 退化且面积为 0。
- 两条切线、弦 AB、Q、M、Q 的准线轨迹和 M 的抛物线轨迹全部进入图层；轨迹绑定相应小问，“全部解析 / 图层”可同时展示完整答案图。
- Python 与浏览器分别独立检查焦点、动态线作用域、切点引用、参数积、Q/M 依赖和轨迹参数；破坏切线引用或轨迹参数会明确显示冲突。
- 电脑端画板新增“隐藏题目栏 / 显示题目栏”，隐藏后解析列和画板自动扩宽；选择保留并可随时恢复，手机端不隐藏输入栏。
- 56 项 Python 后端/核验/部署测试、6 项浏览器可信契约及 PWA 契约通过；真实 Microsoft Edge 已验证在线与断网浏览器四问同构、两角度联动、两条轨迹及宽屏布局，无 JavaScript 异常。

## 0.18.1 · 2026-09-21（北京时间）

- 修复复数切线目标只生成第一条的问题；支持“A、B 两点处的切线”“A 点切线与 B 点切线”“分别过 A、B 作切线”等常见表述。
- A、B 可以是题干直接给出的曲线上点，也可以是直线与圆锥曲线联立得到的精确交点；两条切线分别绑定真实切点、小问编号和曲线引用，并进入可隐藏图层。
- 完整解析同时给出两条切线方程，取各自方向向量计算点积；点积为 0 时给出垂直证明，非 0 时明确指出题设结论不成立。
- 核验层不复用答案结论，而是重新读取两条一般式系数，检查切点引用互异并独立复算 `d₁·d₂`，兼容竖直切线。
- 在线 Python 与断网浏览器使用相同的复数目标识别和双切线契约；52 项后端/核验测试与 2 项部署测试（Python 共 54 项）、6 项浏览器可信契约及 PWA 契约通过，真实 Microsoft Edge 在线/离线流程通过。

## 0.18.0 · 2026-09-21（北京时间）

- 切线不再要求题干直接给出切点坐标：由直线与圆锥曲线联立得到的 A、B 等答案点，也能在当前或后续小问中继续生成精确切线并显示在画板。
- 派生切线保存真实交点对象 ID、曲线引用和小问归属；核验层重新检查切点在曲线上、直线经过切点以及直线法向量与隐式梯度平行，避免同名点串问。
- 新增中点轨迹精确求解：P 在圆、椭圆、双曲线或抛物线上运动，Q 固定、M 为 PQ 中点时，用 `P=2M-Q` 仿射消元得到 M 的轨迹方程。
- 轨迹作为“推导”曲线进入画板和图层，可隐藏/显示；播放联动时 M 随 P 运动，采样位置与精确轨迹一致。
- 轨迹核验同时检查中点依赖图、固定点引用、精确消元式与画板参数；损坏参数会显示冲突，未覆盖轨迹不会用采样拟合冒充答案。
- 在线 Python 与断网浏览器已对计算点切线和轨迹对象执行同题契约对照；覆盖平移纵轴椭圆、圆、抛物线、分数固定点、篡改轨迹和条件不足边界。
- 48 项 Python 后端/核验测试、5 项浏览器可信契约与 PWA 部署契约通过；真实 Microsoft Edge 覆盖在线与断网计算点切线、轨迹图层、联动采样和伪切点引用防护。

## 0.17.0 · 2026-09-21（北京时间）

- 固定直线与圆锥曲线的答案派生图形新增精确交点、弦段和中点；文字答案、画板坐标和长度使用同一次符号联立结果。
- 交点按直线参数稳定排序并保留题设名称；只求交点时不额外添加弦或中点，求弦长、距离或中点时才补充对应对象。
- 每个派生对象使用 `dongjiexi-construction/v1`、稳定引用和小问归属；在线 Python 与断网浏览器输出同一构造结构。
- 核验层独立检查交点同时满足曲线与直线、分支顺序、弦长和中点坐标；运行时引用与构造元数据不一致时，冲突会沿弦和中点依赖链传播。
- 覆盖平移曲线、竖直直线、相切单交点、无实交点、只求交点、多小问隔离及损坏引用等边界。
- 41 项 Python 后端/核验测试、5 项浏览器可信契约与 PWA 部署契约通过；真实 Microsoft Edge 已验证在线/离线交点、弦、中点契约一致性。

## 0.16.1 · 2026-09-21（北京时间）

- AI 场景修复改为可信白名单：题干可确定的曲线、点线和答案派生对象重新生成，模型自行猜测的额外对象不进入画板。
- 手动对象增加依赖闭包校验：自由点以及依赖完整的手动构造继续保留；依赖已丢弃对象、未知构造或引用数量错误的孤立对象不再恢复，并显示明确提示。
- 直线核验统一拒绝非有限系数和退化式 `0x+0y+C=0`，不再回退到另一组浮点参数误判为有效。
- 圆锥曲线结构核验支持只含 `a²`、`b²`、`r²`、`p` 的精确模型；垂足识别同时支持“到直线 l 的垂足”和“到 l 的垂足”。
- 37 项 Python 后端/核验测试、5 项浏览器可信契约与 PWA 部署契约通过；真实 Microsoft Edge 已覆盖答案派生构造、一站式解题、联动运动、结构化参数输入、PC/手机布局和 PWA 离线更新。

## 0.16.0 · 2026-09-21（北京时间）

- 答案派生图形新增圆、椭圆、双曲线和抛物线的曲线上点法线；由同一隐式方程梯度精确生成，并验证经过切点且垂直于切线。
- 新增点到已知直线的垂足：使用正交投影求精确坐标，验证在线和垂直条件，并保存源点、目标直线依赖以支持拖动联动。
- 切线、法线、垂足统一使用 `dongjiexi-construction/v1` 构造元数据，记录类型、输入对象、方法、来源和小问归属。
- 在线 Python 与断网浏览器路径增加逐字段构造契约对照，避免同一题在两种运行方式中显示不同图形；错误法线和伪垂足会被独立核验层判为冲突。
- 修复带明确方程的“直线 l:y=...”被额外误识别成旋转动直线的问题。
- 31 项 Python 后端/核验测试、浏览器与 PWA 契约及真实 Edge 的派生构造、一站式解题、联动运动、全局数学键盘和 PC/手机布局回归通过。

## 0.15.1 · 2026-09-21（北京时间）

- 新增“答案派生图形”：圆、椭圆、双曲线和抛物线在已知曲线上点处的切线，由精确主曲线方程直接推导并加入画板，不再只画题干中已有对象。
- 切线绑定对应小问，图层标记为“推导”，并与答案显示、局部代数核验同步；切换小问时可按需显示或隐藏。
- 切点不在曲线上或切线条件不足时明确标记未完成，不会拿圆锥曲线方程或近似直线冒充切线。
- 修复断网浏览器模式无法从含中文题干的圆方程 `x²+y²=r²` 建图的问题；在线和离线模式均通过真实 Edge 切线回归。
- 24 项 Python 后端/核验测试、浏览器与 PWA 契约及真实 Edge 的答案派生切线、一站式解题、联动运动、全局数学键盘和 PC/手机布局回归通过。

## 0.15.0 · 2026-09-21（北京时间）

- 已覆盖圆锥曲线题改为内置确定性解题优先：条件反推标准方程、特征量、固定直线交点、弦长和中点无需 Qwen/Ollama。
- 题干、符号计算、答案步骤和画板同源；智能增强参数与题干冲突时丢弃错误图形并确定性重建。
- 数学键盘扩展到主参数、方程模板和识别确认字段，并修复原生弹窗内键盘被遮挡的问题。
- 识别曲线上动点、中点和连接线，建立可拖动的依赖图；源点移动时从属点和直线同步重算，浏览器离线模式同样支持。
- 新增“播放联动”，自动驱动动点或动直线并实时重算交点、中点及派生线；暂停后保留当前位置。
- 修正准线方程被误判为曲线标准方程、简单题凭空显示无关动直线等问题。
- 22 项 Python 测试、浏览器/PWA 契约及真实 Edge 的一站式解题、动点/动直线联动与连续播放、参数键盘、PC/手机布局、可信度和离线流程均通过。

## 0.14.0 · 2026-09-20（北京时间）

- 点工具改为独立 x、y 输入，选择“坐标点”后只需填数字，不必写括号与逗号。
- 新增参数式方程模板：直线、圆、椭圆、双曲线、四方向抛物线均可只填必要参数，自动生成方程和图形；高级完整方程输入继续保留。
- 圆支持圆心半径、圆心过点、直径端点和三点定圆；椭圆/双曲线支持半轴、焦距和离心率条件；抛物线支持顶点焦参数与顶点焦点。
- 新增二次、三次、绝对值、反比例、指数、对数、正弦、余弦、正切和根式函数模板。
- 新增离线数学键盘，包含常用运算、函数、圆锥模板、希腊字母、集合和逻辑符号，快捷键为 Ctrl+Shift+K。
- 修复离线条件反推中分数离心率被截断的问题；本机 AI 不可用时仍可由离心率和过点条件反推椭圆。

## 0.13.1 · 2026-09-20（北京时间）

- 识别确认增加普通字段编辑器，可修改曲线类型、方向、中心/顶点、半轴、半径、焦参数和动直线经过条件。
- 支持按“名称, x, y”编辑题设点，按“名称: 方程”编辑题设直线；字段修改与高级模型实时同步。
- 无效参数、坐标、直线或缺失经过点会阻止确认并指出具体行，避免错误模型进入画板。
- 识图、场景确认、题本和解析弹窗补全无障碍标题/说明；动态识别摘要可由屏幕阅读器播报。

## 0.13.0 · 2026-09-20（北京时间）

- PC 端改为“题目输入 → 解析与核验 → 动态画板”三栏工作台，输入与主要动作集中在左栏，推导和参数检查集中在中栏，右栏保留大画板。
- 整题解答成为首要动作；快速建图、文字导入、题图识别与清空入口统一，不再在页面不同位置重复出现。
- 新增三步流程导航、画板工具分组和一键收起，兼顾高中生解题与教师课堂演示。
- 快速建图加入“识别预览与确认”：展示曲线类型、识别方程、中心、方向、参数、动直线经过点与题设对象，确认后才画图。
- 移动端按题目、画板、解析核验的顺序自适应排列，PC 和手机均检查横向溢出。
- 保留 0.12.2 的平移圆锥曲线识别、分母省略写法与题目草稿恢复。

## 0.12.2 · 2026-09-16（北京时间）

- 标准式识别支持平移后的椭圆、双曲线、圆，以及椭圆分母省略写法。
- 识别结果保留中心、方向和直线条件，新增平移椭圆、平移圆与省略分母测试。
- 输入中的未提交题目会自动保存为草稿，可恢复或删除。

## 0.12.0 · 2026-09-16（北京时间）

- 新增手机 PWA、添加到主屏幕、离线画板和安全更新提示。
- 桌面与 Web 共用同一份 `dist` 前端和版本清单；发布后手机检测到新版本并提示原子更新。
- 新增运行时 API 配置，桌面使用本机服务，在线版使用独立 HTTPS 服务；浏览器端不保存模型密钥。
- 服务端支持容器部署、云端 Ollama 地址、端口环境变量和精确 CORS 白名单。
- 新增短期会话鉴权、模型白名单、任务归属、限流和过期清理；私人课堂口令不会作为长期密钥保存在浏览器中。
- 首装离线配置及 404/500/503 均使用已验证的 Web 配置回退，避免错误切换到桌面模式。
- GitHub Pages 同一次构建发布手机 PWA、带 SHA-256 的桌面 ZIP 和更新清单；未配置云端 AI 时明确降级为离线画板。
- 深入推导耗尽输出上限时受控降级一次；条件足以反推曲线时，错误的 AI 绘图参数由确定性结果替换并明确提示。
- 修复根式小数产生极小残差时误报“题设点不在曲线上”。

## 0.11.0 · 2026-09-16（北京时间）

- 新增统一的结构化题目模型：保存曲线、题设点线、小问目标、依赖关系与证明义务。
- 新增确定性三态核验：通过、冲突、未决；覆盖曲线参数、题设点代回、直线与圆锥曲线联立、判别式及二次重根。
- 解析状态拆为“已生成完整作答”和“局部代数核验通过”，不再把 AI 的 `answered` 当作正确性认证。
- 逐问可展开机器核验公式和仍需完成的证明义务；图层标注题干、规则、推导、AI 或手动来源。
- 修复“一个实交点即相切”的错误展示：联立降为一次时明确说明不能据此判相切。
- 识图文字含 `[看不清]` 等未确认字段时，前端与服务端共同阻止解题。
- 服务端不再向页面返回本机路径或模型内部异常详情。
- 本阶段建立可信度基础，不宣称已经验证任意高考题的完整证明；定点、定值和范围结论仍需继续扩展可执行推导树。

## 0.10.0 · 2026-09-15（北京时间）

- 学生逐步学习：隐藏解析与结论、逐步提示、分问进度、重新尝试。
- 我的思路和错因笔记、本机 AI 点评、掌握状态和错题筛选；随本地题稿保存导出。
- 老师讲题模式：图形和推导并排、上下步、键盘翻步、分问切换、大字号与全屏，隐藏个人笔记。
- 红笔屏幕批注、撤销和清除；批注不改变几何对象，不持久化或随图稿打印。
- 不含解析的文字练习页打印，姓名班级栏和作答区。
- AI 后续修正：明确焦点弦、题干点线补齐、曲线上动点关联线、已支持条件的舍入校正、公式转义、并发提交控制。
- 真实模型测试：椭圆由条件反推与斜率积证明、抛物线焦点弦数量积和退化情况、题图转录；仅为样例评测，不能推广为全部题型的准确率保证。

## 0.9.0 · 2026-09-14（北京时间）

- 安装独立的本机模型运行组件，加入整题分问解析、普通/深入模式、进度与取消、模型检测和下载。
- 消除 AI 不可用时静默切换规则模式的行为；快速建图明确不代表完整解答，交点辅助计算不再覆盖证明题结论。
- 分问缺失明确标记；新增已知条件、方法概述、离线 KaTeX 公式排版与大窗口阅读。
- 题图先识别并由使用者核对；追问携带原题、既有解答和最近对话。
- 新增本地题本、搜索、完整题稿导入导出、复制解析与打印/PDF，文字题无图也可保存恢复。
- 输入题干变化时保留原解析并提示，AI 图形参数与原题明确方程冲突时停止载入。
- 本机模型仍可能答错复杂题，完成标记不是证明正确性认证；目前不是公网多人部署版本。


## 0.8.0 · 2026-09-14（北京时间）

- 按用户澄清，把常用动态几何构造直接做进董解析主画板，全部本地运行，不需要打开 GeoGebra。
- 新增点击工具：点、两点直线、线段、射线、圆、中点、垂线、平行线、交点、垂足、测距。
- 依赖构造实时重算；自由源点、可移动构造线可拖动；圆/直线/主圆锥曲线上的点保持约束。
- 对象信息区显示坐标/方程及依赖，支持重命名、隐藏、绑定小问和级联删除；删除可撤销。
- 新增完整 JSON 图稿的保存与打开，保留构造关系；旧 .ggb 草稿入口移至折叠兼容区。
- 修正纵轴双曲线的方程、图形、焦点、渐近线和视图方向。
- 已验证：屏蔽外网时的本地构造、源点/线拖动、约束动点、图稿导出导入、重载、级联撤销、模拟触摸与窄屏；交点相切/不相交/有限线段/射线、循环依赖防护。
- 本轮重点是本地构造；没有把未完成的解题引擎草稿放入发布包，也未宣称解决任意高考题。

## 0.7.1 · 2026-09-14（北京时间）

- 新增版本与更新记录弹窗，展示改动、测试内容和当前能力边界。
- 自由直线交点、弦长和相切单交点已有回归测试。
- 真实更新包的原地安装、数据保留和备份保留已测试。

## 0.7.0 · 2026-09-14（北京时间）

- 官方 GeoGebra Classic 按需嵌入，支持实际几何构造、拖动、独立草稿保存和 .ggb 导入导出。
- 本地 Pointer Events 拖动自由点、直线、圆与曲线控制点；依赖连线、交点和弦长动态重算。
- 撤销重做、坐标吸附、平移视野、鼠标缩放、窄屏布局。
- 修复题干中的点线提取、焦点绑定、小问归属、重复切点，以及自由直线未绘制的问题。
- 三栏重新排版，画板固定在主要操作区域，侧栏独立滚动。
- 原地 ZIP 更新、版本检查、带时间戳的备份；桌面快捷方式不用更换。

## 尚未完成

- 任意文字难题的完整、可靠、无需人工核对的自动证明。
- 任意多曲线构造从 GeoGebra 到本地题目解析的双向同步。
- 真实手机、多媒体型号上的兼容性验收。
- 在线自动发布通道。

以上是工程待办，不是已实现功能承诺。
